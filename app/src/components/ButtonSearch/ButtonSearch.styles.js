import { StyleSheet } from 'aphrodite';

const Styles = StyleSheet.create({
    defaultBtn: {
        border: 'none',
        borderRadius: 0,
        outline: 'transparent',
    },
});

export default Styles;
